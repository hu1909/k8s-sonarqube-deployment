apiVersion: v1
kind: PersistentVolumeClaim 
metadata:
  name: postgres-pvc
  namespace: monitoring
spec:
  storageClassName: azure-disk
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
       storage: 10Gi
